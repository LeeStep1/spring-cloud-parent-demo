<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bit.module.manager.dao.CompanyDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.bit.module.manager.bean.Company" id="companyMap">
        <result property="id" column="id"/>
        <result property="companyName" column="company_name"/>
        <result property="level" column="level"/>
        <result property="parentId" column="parent_id"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        company_name,
        level,
        parent_id
    </sql>

    <!-- 根据主键查询数据 -->
    <select id="getCompanyById" parameterType="Integer" resultMap="companyMap">
        select
        <include refid="Base_Column_List"/>
        from t_company
        where id = #{id,jdbcType=BIGINT}
    </select>

    <!-- 多参数查询数据 -->
    <select id="findByParam" parameterType="com.bit.module.manager.bean.Company" resultMap="companyMap">
        select
        <include refid="Base_Column_List"/>
        from t_company
        <where>
            <if test="companyName!=null and companyName!=''">
                and company_name = #{companyName,jdbcType=VARCHAR}
            </if>
            <if test="level!=null">
                and level = #{level,jdbcType=INTEGER}
            </if>
            <if test="parentId!=null">
                and parent_id = #{parentId,jdbcType=BIGINT}
            </if>

        </where>
    </select>


    <!-- 新增数据 -->
    <insert id="addCompany" parameterType="com.bit.module.manager.bean.Company" keyColumn="id"
            useGeneratedKeys="true">
        insert into t_company
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="companyName!=null and companyName!=''">
                company_name,
            </if>
            <if test="level!=null">
                level,
            </if>
            <if test="parentId!=null">
                parent_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="companyName!=null and companyName!=''">
                #{companyName,jdbcType=VARCHAR},
            </if>
            <if test="level!=null">
                #{level,jdbcType=INTEGER},
            </if>
            <if test="parentId!=null">
                #{parentId,jdbcType=BIGINT},
            </if>
        </trim>
    </insert>


    <!-- 编辑数据 -->
    <update id="updateCompany" parameterType="com.bit.module.manager.bean.Company">
        update t_company
        <trim prefix="set" suffixOverrides=",">
            <if test="companyName!=null and companyName!=''">
                company_name = #{companyName,jdbcType=VARCHAR},
            </if>
            <if test="level!=null">
                level = #{level,jdbcType=INTEGER},
            </if>
            <if test="parentId!=null">
                parent_id = #{parentId,jdbcType=BIGINT},
            </if>
        </trim>
        where id = #{id,jdbcType=BIGINT}
    </update>


    <!-- 删除数据 -->
    <delete id="delCompanyById" parameterType="Integer">
        delete from t_company
        where id = #{id,jdbcType=BIGINT}
    </delete>


</mapper>