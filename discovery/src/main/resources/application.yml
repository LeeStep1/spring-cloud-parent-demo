server:
  port: 9090
spring:
  application:
    name: discovery
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://221.198.184.55:${server.port}/eureka/